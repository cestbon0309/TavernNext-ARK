[package]
name = "tavern_tokenizer_ffi"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[dependencies]
miktik = { path = "../../third_party/miktik", default-features = false, features = ["openai"] }
